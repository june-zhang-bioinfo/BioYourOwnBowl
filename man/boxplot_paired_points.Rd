% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/customized_vis.R
\name{boxplot_paired_points}
\alias{boxplot_paired_points}
\title{Plot Paired Gene Expression with Links and Faceting}
\usage{
boxplot_paired_points(
  object,
  gene,
  timepoint_col = "Timepoint",
  participant_col = "Patient",
  pre_label = "Baseline",
  post_label = "Post-Treatment",
  facet_col = NULL,
  value_type = "mean"
)
}
\arguments{
\item{object}{A Seurat object containing expression data and metadata.}

\item{gene}{Character string specifying the gene to plot.}

\item{timepoint_col}{Character string specifying the metadata column for timepoints (e.g., "Timepoint").}

\item{participant_col}{Character string specifying the metadata column for patient ID (e.g., "Patient").}

\item{pre_label}{Character string for the 'pre' timepoint label (e.g., "Baseline").}

\item{post_label}{Character string for the 'post' timepoint label (e.g., "Post-Treatment").}

\item{facet_col}{Character string specifying the metadata column to facet by (e.g., "Cohort"). Default is NULL.}

\item{value_type}{Character string indicating whether to plot the mean ("mean") or median ("median")
expression per patient. Default is "mean".}
}
\value{
A ggplot object showing paired expression changes.
}
\description{
Generates boxplots of gene expression (pre vs. post timepoint) with
individual data points linked for paired samples. Supports faceting by cohort.
}
