% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/customized_vis.R
\name{scatter_plot}
\alias{scatter_plot}
\title{Scatter Plot of Two Variables from a Seurat Object}
\usage{
scatter_plot(
  object,
  x_var,
  y_var,
  color_by,
  colors = NULL,
  point_size = 0.5,
  alpha = 0.5,
  title = NULL,
  downsample = FALSE,
  shuffle = TRUE
)

scatter_plot(
  object,
  x_var,
  y_var,
  color_by,
  colors = NULL,
  point_size = 0.5,
  alpha = 0.5,
  title = NULL,
  downsample = FALSE,
  shuffle = TRUE
)
}
\arguments{
\item{object}{A Seurat object.}

\item{x_var}{Character. Column name in \code{meta.data} or gene name for the x-axis.}

\item{y_var}{Character. Column name in \code{meta.data} or gene name for the y-axis.}

\item{color_by}{Character. Column name in \code{meta.data} to color points by.}

\item{colors}{Character vector of colors. If \code{NULL}, uses default ggplot2 colors (default = NULL).}

\item{point_size}{Numeric. Size of points (default = 0.5).}

\item{alpha}{Numeric. Transparency of points, between 0 and 1 (default = 0.5).}

\item{title}{Character. Plot title. If \code{NULL}, auto-generated from x_var and y_var (default = NULL).}

\item{downsample}{Logical. If \code{TRUE}, randomly downsample each group to the size of the
smallest group before plotting (default = FALSE).}

\item{shuffle}{Logical. If \code{TRUE}, randomly shuffles the order of points before plotting
to avoid overplotting by group order (default = TRUE).}
}
\value{
A \code{ggplot} object.

A \code{ggplot} object.
}
\description{
Plots two variables against each other, coloring points by a specified grouping variable.
Variables can be meta.data columns or gene expression values (from the data layer).

Plots two variables against each other, coloring points by a specified grouping variable.
Variables can be meta.data columns or gene expression values (from the data layer).
}
